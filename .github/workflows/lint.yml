name: Code quality check

on:
  push:
    branches: ["main"]
  pull_request:

jobs:
  code-quality:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
    steps:
    - uses: actions/checkout@v4
    - name: Setup Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: "3.9"
    - name: Install dependencies
      run: |
        pip install -e .
        pip install -r requirements-lint.txt
    - name: Run lint check
      continue-on-error: true  # Allows failure. Remove once hard fail is required.
      run: ruff check
